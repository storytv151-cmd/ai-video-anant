# Tests

This directory is reserved for automated tests.

## Structure

- `unit/` for isolated unit tests
- `integration/` for API and infrastructure integration tests

## Future Scope

Future phases can add test runners, fixtures, mocks, and test utilities here.