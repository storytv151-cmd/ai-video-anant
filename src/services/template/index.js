/**
 * Template service placeholder.
 * Template management workflows will be added in a future phase.
 */
const templateService = Object.freeze({
  name: 'template-service-placeholder',
  status: 'not-implemented',
});

export default templateService;