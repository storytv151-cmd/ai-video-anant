/**
 * Base video provider placeholder.
 * Shared provider contracts and common behaviors will be defined in a future phase.
 */
class BaseVideoProvider {
  constructor(providerName = 'base-video-provider-placeholder') {
    this.providerName = providerName;
    this.status = 'not-implemented';
  }
}

export default BaseVideoProvider;