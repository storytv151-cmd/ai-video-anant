/**
 * Generation service placeholder.
 * Video generation orchestration will be added in a future phase.
 */
const generationService = Object.freeze({
  name: 'generation-service-placeholder',
  status: 'not-implemented',
});

export default generationService;