/**
 * Cron registry placeholder.
 * Scheduled orchestration will be registered here when enabled in later phases.
 */
const cronRegistry = Object.freeze({
  jobs: [],
});

export default cronRegistry;
