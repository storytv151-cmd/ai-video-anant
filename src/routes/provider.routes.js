/**
 * Provider route placeholder.
 * Endpoints for this module will be added in a future phase.
 */
import { Router } from 'express';

const providerRouter = Router();

export default providerRouter;