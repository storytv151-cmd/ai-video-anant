/**
 * Generation route placeholder.
 * Endpoints for this module will be added in a future phase.
 */
import { Router } from 'express';

const generationRouter = Router();

export default generationRouter;