/**
 * Swagger configuration placeholder.
 * API documentation generation can be integrated here in a future phase.
 */
const swaggerConfig = Object.freeze({
  enabled: false,
  status: "not-implemented",
});

export default swaggerConfig;
