/**
 * Template validator placeholder.
 * Validation rules will be added in a future phase.
 */
const template_validator = Object.freeze({
  name: 'template_validator',
  status: 'not-implemented',
});

export default template_validator;