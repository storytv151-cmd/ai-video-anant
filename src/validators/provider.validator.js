/**
 * Provider validator placeholder.
 * Validation rules will be added in a future phase.
 */
const provider_validator = Object.freeze({
  name: 'provider_validator',
  status: 'not-implemented',
});

export default provider_validator;