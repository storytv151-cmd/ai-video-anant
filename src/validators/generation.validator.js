/**
 * Generation validator placeholder.
 * Validation rules will be added in a future phase.
 */
const generation_validator = Object.freeze({
  name: 'generation_validator',
  status: 'not-implemented',
});

export default generation_validator;