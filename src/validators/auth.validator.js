/**
 * Authentication validator placeholder.
 * Validation rules will be added in a future phase.
 */
const auth_validator = Object.freeze({
  name: 'auth_validator',
  status: 'not-implemented',
});

export default auth_validator;