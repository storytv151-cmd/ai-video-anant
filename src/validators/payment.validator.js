/**
 * Payment validator placeholder.
 * Validation rules will be added in a future phase.
 */
const payment_validator = Object.freeze({
  name: 'payment_validator',
  status: 'not-implemented',
});

export default payment_validator;