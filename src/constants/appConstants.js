/**
 * Application constant placeholders.
 * Shared application-level constants can be expanded here in future phases.
 */
const APP_CONSTANTS = Object.freeze({
  APP_NAMESPACE: "ai-video-generation-platform",
});

export default APP_CONSTANTS;
