/**
 * Provider name constants placeholder.
 * Supported AI provider identifiers will be expanded in a future phase.
 */
const PROVIDER_NAMES = Object.freeze({
  NANO_BANANA: 'nano-banana',
  KLING: 'kling',
  PIKA: 'pika',
  RUNWAY: 'runway',
  LUMA: 'luma',
});

export default PROVIDER_NAMES;