/**
 * Feature flag constants placeholder.
 * Runtime feature switches can be registered here in future phases.
 */
const FEATURE_FLAGS = Object.freeze({
  ENABLE_EXPERIMENTAL_FEATURES: false,
});

export default FEATURE_FLAGS;