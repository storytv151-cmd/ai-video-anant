/**
 * UserDevice model placeholder.
 * The Mongoose schema for this resource will be defined in a future phase.
 */
const UserDeviceModel = Object.freeze({
  name: 'UserDevice',
  status: 'schema-not-implemented',
});

export default UserDeviceModel;